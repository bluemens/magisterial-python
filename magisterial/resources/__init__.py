# Resource namespaces for the Magisterial client. Each module carries the
# sync and async variants of one API area; method names mirror the API's
# operationIds (see https://api.magisterial.ai/v1/openapi.json).
